parameters:  
  dependsOn: []
  agentcapability: []
  agentDemand: []
  Version: ''

jobs:
- job: MSBuild  
  dependsOn: ${{ parameters.dependsOn }}
  condition: and(succeeded(), eq(variables['MS_Build'], 'true'))
  pool:
    name: '$(PoolName)'
    demands:
    - ${{ parameters.agentDemand }} -equals ${{ parameters.agentcapability }}
  steps:
  - checkout: none

  - task: PowerShell@2
    inputs:
      filePath: $(Script_nuGet_restore) 
      arguments: '$(build.sourcesdirectory) $(Project) $(NugetPath) '
    displayName: 'Package Restore'  

  - task: bleddynrichards.Assembly-Info-Task.Assembly-Info-Task.Assembly-Info-NetFramework@2
    displayName: ' File Versioning '
    inputs:
      FileNames: | 
       **\AssemblyInfo.cs
      Title: $(ComponentName)
      Product: $(ComponentName)
      Company: Philips
      VersionNumber: 1.0.0.0
      FileVersionNumber: 1.0.0.0
      InformationalVersion: 1.0.0.0'
      Copyright: 2021
    enabled: 'True'

  - task: MSBuild@1
    displayName: 'Build solution: $(ComponentName)'    
    inputs:
      solution: $(SolutionPath) 
      msbuildVersion: '16.0' 
      msbuildArchitecture: 'x64' 
      configuration: $(configuration)
      clean: true                
